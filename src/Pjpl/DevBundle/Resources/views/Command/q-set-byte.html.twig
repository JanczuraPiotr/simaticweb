{% extends "PjplDevBundle::window.html.twig" %}

{% block body %}
	CommandController::qSetByteAction()
	<hr>
	{% if response != null %}
		wartość w porcie : {{ response }}
	{% endif %}
	<hr>
	{{ form_start(form) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
{% endblock %}